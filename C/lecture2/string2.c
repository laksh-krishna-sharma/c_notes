#include<stdio.h>
int main()
{
    char ch [50];
    // thrie is a problem with scanf agar mai input mai 'hi i am laksh' du to wo sirf hi input le ga aur wahi print kre ga hi ke baad jo space ayaga waha se kuch input nahi ho ga and this problem can be solved by gets(for input) and puts(for output)
    gets(ch);
    puts(ch);

    return 0;
}